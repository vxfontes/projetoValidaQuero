import { Grid, styled } from "@mui/material";
import theme from "../theme";

export const TemplateContainerGrid = styled(Grid)({
    background: theme.palette.fundoComponente?.main,
    borderRadius: '14px'
})